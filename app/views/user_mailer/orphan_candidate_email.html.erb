<p>
  We found <%= @blocks.count %> blocks at height <%= @orphan_candidate.height %>:
  <ul>
    <% @blocks.each do | block | %>
      <li>
        <%= block.block_hash %>
        <% if block.pool.present? %>
          (<%= block.pool %>)
        <% else %>
          (unknown pool)
        <% end %>
      </li>
    <% end %>
  </ul>
</p>

<p>
  For more information, see: <%= nodes_for_coin_url(@orphan_candidate.coin.downcase) %>
</p>
